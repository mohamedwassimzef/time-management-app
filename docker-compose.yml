services:
  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=mongodb+srv://mohamed_wassim:zauegbf684@cluster0.nfuoyxw.mongodb.net/timeManager?appName=Cluster0

    


